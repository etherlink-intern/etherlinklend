# Architecture

This repository is an Etherlink-oriented scaffold around pinned Morpho Blue-compatible upstream components. Morpho core, IRMs, and oracle implementations are referenced as submodules. Local source directories are reserved for future adapters, Etherlink-specific periphery, and reviewed oracle integration code.
