# Scripts

Scripts are safety placeholders. They compile and enforce explicit confirmation before broadcast mode, but they do not deploy Morpho core, configure real markets, or write production artifacts. Confirm the upstream deployment path and security process before replacing placeholders.
