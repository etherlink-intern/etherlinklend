# Deployment Records

Deployment artifacts belong under `deployments/<chain>/` and must include transaction hashes, bytecode metadata, constructor arguments, compiler settings, verification links, operators, and reviewer signatures. Never store private keys or secret RPC credentials here.
