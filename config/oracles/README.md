# Oracle Configuration

No production oracle adapter is implemented in this scaffold. Every feed ID/address, heartbeat, deviation threshold, and decimals normalization path must be verified from official oracle documentation before market creation.
